# NPM Configuration
# Optimizations and settings for this project

# Ensure exact versions are used
save-exact=false

# Use legacy peer deps handling for compatibility
legacy-peer-deps=false

# Progress bar coloring
progress=true

# Security audit level
audit-level=moderate
